load("@pip//:requirements.bzl", "requirement")

py_library(
    name = "models",
    srcs = [
        "__init__.py",
        "gpt.py",
    ],
    visibility = ["//visibility:public"],
    deps = [
        requirement("torch"),
    ],
)
